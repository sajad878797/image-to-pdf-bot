{
  "build": {
    "builder": "NIXPACKS",
    "nixpacksConfig": {
      "phases": {
        "install": {
          "commands": ["pip install -r requirements.txt"]
        },
        "start": {
          "cmd": "./start.sh"
        }
      }
    }
  }
}
